{
  "name": "@repo/convex",
  "version": "0.0.0",
  "private": true,
  "main": "./index.ts",
  "types": "./index.ts",
  "scripts": {
    "dev": "convex dev",
    "build": "test -n \"$CONVEX_DEPLOYMENT\" && convex codegen --typecheck disable || echo 'Skipping convex codegen (no CONVEX_DEPLOYMENT)'",
    "clean": "git clean -xdf .cache .turbo node_modules",
    "typecheck": "tsc --noEmit --emitDeclarationOnly false"
  },
  "dependencies": {
    "@t3-oss/env-nextjs": "^0.13.8",
    "convex": "^1.31.7",
    "server-only": "^0.0.1",
    "zod": "^4.1.13"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "typescript": "^5.9.3"
  }
}
